# Port-Forward Manager

GUI desktop per gestire `kubectl port-forward` verso microservizi in un cluster Kubernetes.

## Requisiti

- **Python 3.7+** (richiesto per `subprocess.CREATE_NO_WINDOW` su Windows)
- **kubectl** installato e disponibile nel PATH
